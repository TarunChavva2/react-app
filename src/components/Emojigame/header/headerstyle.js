import styled from '@emotion/styled';
import tw from 'tailwind.macro';


let Headerdiv = styled.div`${tw`flex items-center justify-between h-23 font-mono bg-white`}`;
let Rightheader = styled.div`${tw`flex justify-around items-center`}`;
let Themebutton = styled.button`${tw`border-none`}`
export { Headerdiv, Rightheader, Themebutton };